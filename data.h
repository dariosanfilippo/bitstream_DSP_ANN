#ifndef DATA
#define DATA

void freq_est_data(Sig* in, Sig* out);
void amp_est_data(Sig* in, Sig* out);
void add_data(Sig* in0, Sig* in1, Sig* out);
void mul_data(Sig* in0, Sig* in1, Sig* out);

#endif
